# Kodi automatic training tests

Caller AI plays realistic callers, Kodi uses the production prompt, and Judge AI scores each transcript and tool choice. Failures are saved as JSON reports.

Run `npm run train:kodi`. The suite never edits prompts or deploys production automatically.
